# Skills 分享与代码背景

## 第一章：背景

### 1.1 为什么不能直接用 Spark SQL 完成 KMeans

Spark SQL 主要用于查询和处理结构化数据，例如筛选、聚合、关联和写入数据。它本身没有提供可直接配置和执行 KMeans 模型训练的 SQL 语法。

KMeans 不只是一次数据查询，还需要根据特征列和算法参数进行模型训练，再使用训练好的模型生成聚类结果。这类“训练模型并进行预测”的过程，通常需要调用 Spark ML 的 DataFrame API，不能直接通过普通 `spark.sql()` 完成。

### 1.2 如何做到只写 SQL 就能完成 KMeans

项目在 Spark SQL 执行框架中增加了一个 `KMEANS` action。用户只需要在 SQL 配置文件中完成以下几步：

1. 使用 SQL 准备 KMeans 的输入数据，并创建临时视图；
2. 声明 `KMEANS` action，配置输入视图、特征列、聚类数量和迭代次数等参数；
3. 使用后续 SQL 读取 KMeans 生成的结果视图，并将结果写入目标表。

执行流程如下：

```text
SQL 准备数据
    -> KMEANS action
    -> 框架调用 Spark ML 完成训练和预测
    -> 生成结果临时视图
    -> 后续 SQL 继续处理结果
```

这样，用户不需要编写 Java、Scala 或 Python 代码，算法执行细节由计算框架负责，用户通过 SQL 配置即可完成 KMeans 任务。

## 第二章：想法

### 2.1 使用 Skills 扩展 MLlib 算法能力

KMeans 只是 MLlib 中的一种算法。后续可以通过 Skills 描述不同算法的使用方式、参数配置和代码改造模式，让 AI Agent 能够结合现有项目自动修改计算框架，生成对应的算法执行代码。

这样，新增一种 MLlib 算法时，用户只需要说明业务需求和预期配置，Agent 就可以根据 Skill 分析现有框架，补充配置模型、执行逻辑和 SQL action，而不需要完全手动开发。

### 2.2 逻辑回归的预期模式

后续计划使用逻辑回归作为下一个示例。逻辑回归任务可以准备两张表：

- 训练表：包含特征列和真实结果列，用于训练模型；
- 预测表：包含需要预测的数据特征，用于生成预测结果。

用户可以在 SQL 配置中声明一个 `LOGISTIC_REGRESSION` action，配置训练表、预测表、特征列、目标列、算法参数以及输出视图名称。框架负责完成模型训练和预测，并将结果注册为临时视图，后续 Spark SQL 可以继续读取这个结果视图。

执行流程如下：

```text
SQL 准备训练表和预测表
    -> LOGISTIC_REGRESSION action
    -> 框架调用 Spark ML 训练模型
    -> 使用模型生成 prediction 和 probability
    -> 注册结果临时视图
    -> 后续 Spark SQL 继续处理
```

完成相关 Skills 后，Agent 可以根据这套模式自动生成逻辑回归的计算框架代码。之后，用户就可以使用新的计算框架，通过 SQL 编排可配置的逻辑回归任务，而不需要手动编写完整的模型训练代码。

后续将以逻辑回归为例，展示 Skills 如何帮助 Agent 理解需求、改造计算框架并生成可运行代码，从而体现 Skills 在算法扩展场景中的实际效果。
